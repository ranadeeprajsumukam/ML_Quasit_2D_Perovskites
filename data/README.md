# Data

Place your Excel workbook here:

- **File name:** `SF_DATA_EMISSION_PEAK.xlsx`
- **Sheets used:**
  - `SF_DATA_EMISSION_PEAK_NEW` — emission peak / photon energy models
  - `EQE_CLEANED` — EQE models

Copy from your local path (if you used the original notebooks):

```
C:\Users\srika\OneDrive\Desktop\SF_DATA_EMISSION_PEAK.xlsx
```

The workbook is listed in `.gitignore` so it is not pushed to GitHub by default. Remove that line from `.gitignore` only if you intend to version the dataset in the repository.
